name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy files via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 46.224.73.134
          username: lookuply
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "index.html,logo.svg,translations.json,favicon.svg,og-image.svg,robots.txt,sitemap.xml"
          target: "/tmp/lookuply-landing/"

      - name: Deploy files
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 46.224.73.134
          username: lookuply
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo cp /tmp/lookuply-landing/index.html /var/www/html/index.html
            sudo cp /tmp/lookuply-landing/logo.svg /var/www/html/logo.svg
            sudo cp /tmp/lookuply-landing/translations.json /var/www/html/translations.json
            sudo cp /tmp/lookuply-landing/favicon.svg /var/www/html/favicon.svg
            sudo cp /tmp/lookuply-landing/og-image.svg /var/www/html/og-image.svg
            sudo cp /tmp/lookuply-landing/robots.txt /var/www/html/robots.txt
            sudo cp /tmp/lookuply-landing/sitemap.xml /var/www/html/sitemap.xml
            sudo chown www-data:www-data /var/www/html/index.html /var/www/html/logo.svg /var/www/html/translations.json /var/www/html/favicon.svg /var/www/html/og-image.svg /var/www/html/robots.txt /var/www/html/sitemap.xml
            sudo chmod 644 /var/www/html/index.html /var/www/html/logo.svg /var/www/html/translations.json /var/www/html/favicon.svg /var/www/html/og-image.svg /var/www/html/robots.txt /var/www/html/sitemap.xml
            echo "✅ Deployment successful!"
